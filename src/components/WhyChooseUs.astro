---
const reasons = [
  {
    title: "Expertise Technique",
    description: "Des études approfondies et une équipe qualifiée pour une mise en service optimale.",
    icon: "engineering" // mapping interne
  },
  {
    title: "Large Gamme de Produits",
    description: "Tout pour le BTP : climatisation, plomberie, carrelage et équipements sanitaires.",
    icon: "inventory"
  },
  {
    title: "Formation Professionnelle",
    description: "Un centre d'excellence dédié à la qualification de la main-d'œuvre locale.",
    icon: "school"
  },
  {
    title: "Flexibilité et Location",
    description: "Des options de location et des solutions adaptées à vos contraintes budgétaires.",
    icon: "handshake"
  }
];
---

<section class="py-16 bg-[var(--color-secondary)] text-white relative overflow-hidden">
  
  <div class="absolute top-0 left-0 w-full h-full opacity-10 pointer-events-none">
    <svg class="h-full w-full" viewBox="0 0 100 100" preserveAspectRatio="none">
        <path d="M0 100 C 20 0 50 0 100 100 Z" fill="white" />
    </svg>
  </div>

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 relative z-10">
    
    <div class="text-center mb-16">
      <h2 class="text-3xl md:text-4xl font-bold mb-4">
        En bref, voici quatre bonnes raisons de nous choisir
      </h2>
      <div class="h-1 w-24 bg-[var(--color-primary)] mx-auto"></div>
    </div>

    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
      {reasons.map((item) => (
        <div class="bg-white/5 backdrop-blur-sm border border-white/10 p-6 rounded-xl hover:bg-white/10 transition duration-300 group">
          
          <div class="h-14 w-14 bg-[var(--color-primary)] rounded-lg flex items-center justify-center mb-6 text-white shadow-lg group-hover:scale-110 transition-transform duration-300">
            {item.icon === 'engineering' && (
                <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M10 20l4-16m4 4l4 4-4 4M6 16l-4-4 4-4" /></svg>
            )}
            {item.icon === 'inventory' && (
                <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4" /></svg>
            )}
            {item.icon === 'school' && (
                <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 14l9-5-9-5-9 5 9 5z" /><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 14l6.16-3.422a12.083 12.083 0 01.665 6.479A11.952 11.952 0 0012 20.055a11.952 11.952 0 00-6.824-2.998 12.078 12.078 0 01.665-6.479L12 14z" /></svg>
            )}
            {item.icon === 'handshake' && (
                <svg class="w-8 h-8" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z" /></svg>
            )}
          </div>

          <h3 class="text-xl font-bold text-white mb-3">
            {item.title}
          </h3>
          
          <p class="text-gray-300 text-sm leading-relaxed">
            {item.description}
          </p>
        </div>
      ))}
    </div>

  </div>
</section>